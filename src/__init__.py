##this is an empty file but required to exist in the src directory to treat the src directory as a package. 
# This enables import lines that use dot notation like:
# from src.irrigation_base import DB 
#where a module (DB) from the package (src) is imported to the current file. 